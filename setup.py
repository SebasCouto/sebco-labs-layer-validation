from setuptools import setup, find_packages

setup(
    name='sebco-labs-layer-validation',
    version='0.1',
    packages=find_packages(),  # No es necesario el argumento 'where' porque la estructura coincide ahora
    install_requires=[
        # tus dependencias aquí
    ],
)
